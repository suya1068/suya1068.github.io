{% extends "template/public.twig" %}

{% block head_assets %}
    <link rel="stylesheet" href="{{ manifest['products/products-list.css'] }}" />
{% endblock %}

{% block meta_data %}
    <meta id="product-data" content="{{ products['result'] }}">
    <meta id="category-data" content="{{ category_list }}">
    <meta id="params" content="{{ params }}">
    {#<meta id="time_flag" content="{{ time_flag }}">#}
{% endblock %}

{% block body_assets %}
    <script src="{{ manifest['products/products-list.js'] }}"></script>
{% endblock %}
