const base = "/cs";

const navUrlData = [
    { baseUrl: base, restUrl: "/qna", className: "nav-nonSelect", navTitle: "자주 묻는 질문" }
    // { baseUrl: base, restUrl: "/notice", className: "nav-nonSelect", navTitle: "공지사항" }
];

export default navUrlData;
